import{readFile,access}from'node:fs/promises';import{join}from'node:path';const file=join(process.cwd(),'docs','evidence','requirements.json'),allowed=new Set(['verified','externally-blocked','not-applicable-with-reason']);const data=JSON.parse(await readFile(file,'utf8'));const failures=[];for(const item of data.requirements){if(!allowed.has(item.state))failures.push(`${item.id}: ${item.state}`);if(item.state==='verified'){if(!item.evidence?.length)failures.push(`${item.id}: missing evidence`);for(const evidence of item.evidence)try{await access(join(process.cwd(),evidence))}catch{failures.push(`${item.id}: missing ${evidence}`)}}if(item.state!=='verified'&&!item.reason)failures.push(`${item.id}: missing reason`)}console.log(JSON.stringify({total:data.requirements.length,failures},null,2));if(failures.length)process.exitCode=1;
