const enabled=process.env.AUTHORIZED_ACTIVE_TESTING==='true',target=process.env.AUTHORIZED_STAGING_URL;function approved(value){try{const u=new URL(value);return u.protocol==='https:'&&!/^(localhost|127\.|10\.|192\.168\.|169\.254\.|172\.(1[6-9]|2\d|3[01])\.)/i.test(u.hostname)}catch{return false}}if(!enabled||!approved(target)){console.error('Refusing active security checks: set AUTHORIZED_ACTIVE_TESTING=true and an owned HTTPS AUTHORIZED_STAGING_URL.');process.exit(2)}console.log(`Authorized target accepted: ${new URL(target).origin}. Install and invoke approved ZAP/Nuclei tooling separately; this guard never scans prospects.`);
