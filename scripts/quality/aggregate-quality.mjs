import{mkdir,writeFile}from'node:fs/promises';import{join}from'node:path';import{BUDGETS,STANDARD_THRESHOLDS,CINEMATIC_THRESHOLDS}from'./constants.mjs';const out=join(process.cwd(),'docs','evidence','quality');await mkdir(out,{recursive:true});await writeFile(join(out,'quality-contract.json'),JSON.stringify({generatedAt:new Date().toISOString(),thresholds:{standard:STANDARD_THRESHOLDS,cinematic:CINEMATIC_THRESHOLDS},budgets:BUDGETS,fieldData:'No Core Web Vitals field data is claimed by this local artifact.'},null,2));
