# Authorized load evidence

The conservative k6 model is implemented but not claimed as run. It refuses to start without an explicit owned HTTPS staging authorization and never loops checkout or creates Stripe sessions.
