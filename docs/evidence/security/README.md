# Authorized security evidence

Local source/secret/header checks are stored separately from active testing. No active scan is claimed. Active tools require `AUTHORIZED_ACTIVE_TESTING=true` and an explicitly owned HTTPS staging URL; prospects and third-party sites are forbidden targets.
