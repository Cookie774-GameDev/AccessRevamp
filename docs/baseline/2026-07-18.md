# AccessRevamp pre-rebuild baseline — 2026-07-18

**Status:** Local pre-rebuild baseline captured; no remote deployment or live-system claim

**Reference commit:** `0339bd3` (`docs: plan production rebuild execution`)

## Verified locally before implementation

- Linked worktree: `C:\Users\viper\projects\AccessRevamp-worktrees\editorial-story-ultramarine`.
- Branch: `redesign/editorial-story-ultramarine-local`.
- At the reference commit, the branch was eight commits ahead of its upstream and zero behind.
- Node: 24.16.0; package floor: Node 22.12.0.
- Existing suite: 67 tests, 67 passed, 0 failed before the new foundation contract test was added.
- `npm run check`: passed before the production-rebuild planning stage.
- `npm audit --omit=dev --audit-level=high`: zero vulnerabilities at the planning baseline.
- Existing build: CSS approximately 7.57 KB gzip; JavaScript approximately 70.59 KB gzip.
- Installed Playwright engines: Chromium, Firefox, and WebKit caches were present locally.
- Current source routes include `/`, `/work`, `/work/:slug`, `/services`, `/process`, `/pricing`, `/sample-report`, `/methodology`, `/cinematic-scroll`, `/contact`, auth, dashboard, legal/policy, success, and cancel routes.
- Current payment catalog is stale: Homepage Reveal $50, Quick Fix $199, and Cinematic Scroll Site $250. Stripe identifiers/direct test links are present in browser configuration and must be removed.
- Current checkout is anonymous, accepts a browser plan key, and has no entitlement reservation.
- Current webhook verifies price/amount/currency but has no cumulative entitlement or refund dependency reconciliation.
- Current migrations have RLS-protected customer orders/projects/refund requests and service-only operational tables, but no `tier_catalog`, `entitlements`, `upgrade_reservations`, or `refund_dependencies`.

## Captured browser evidence

`npm run baseline` completed against a programmatically started, identity-verified Vite production preview on a generated loopback port. The run recorded 16 screenshots and 16 sanitized axe reports across `/`, `/pricing`, `/work`, and `/cinematic-scroll` at widths 375, 768, 1024, and 1440 CSS pixels. Every navigation returned HTTP 200 with zero captured console errors and zero failed requests.

Lighthouse 13.4.0 produced these baseline scores:

| Route | Performance | Accessibility | Best practices | SEO |
| --- | ---: | ---: | ---: | ---: |
| `/` | 81 | 95 | 100 | 91 |
| `/pricing` | 89 | 93 | 100 | 91 |
| `/work` | 92 | 98 | 100 | 91 |
| `/cinematic-scroll` | 27 | 100 | 100 | 91 |

Lighthouse saved each valid report before its Windows Chrome-profile cleanup raised `EPERM`; the harness classifies that post-capture condition as a cleanup warning and preserves the valid report. The axe baseline found `color-contrast` on the home and pricing routes and `heading-order` on pricing and work at every tested width. Cinematic scroll had no axe violations. These are pre-rebuild defects, not accepted final-state exceptions.

The final evidence directory contains 16 PNG files and 22 JSON files. Its exact forbidden-pattern scan is clean for `sk_`, `whsec_`, `service_role`, `price_`, and `book.stripe.com`; browser-derived JSON is recursively sanitized before writing.

## Not verified

- No Netlify preview/staging URL has been verified for this branch.
- No production-domain deployment has been authorized or performed.
- No remote Supabase schema/project/migration state has been verified.
- No Stripe test product/price creation or end-to-end webhook transaction has been verified for the new $50/$200/$250 catalog.
- Stripe live mode is not authorized and has not been tested.
- No owned staging authorization is present for ZAP, Nuclei, k6, or Locust.
- No outreach was sent; `sending_enabled` remains false.

Missing remote evidence remains incomplete or externally blocked. It is not inferred from source code.
