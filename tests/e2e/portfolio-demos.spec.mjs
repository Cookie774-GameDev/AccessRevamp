import{test,expect}from'playwright/test';const demos=['greenline-lawn-and-grounds','firejar-spicy-peanut-butter','clearflow-plumbing'];for(const slug of demos)test(`${slug} is an honest interactive demo`,async({page})=>{await page.goto(`/portfolio/${slug}`);await expect(page.getByText('Original working demo — not a client engagement.')).toBeVisible();await expect(page.getByRole('heading',{name:'Design rationale'})).toBeVisible();await expect(page.getByRole('heading',{name:'Accessibility notes'})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth)).toBeLessThanOrEqual(1)});
test('Greenline sample quote completes without network fulfillment',async({page})=>{const posts=[];page.on('request',request=>{if(request.method()!=='GET')posts.push(request.url())});await page.goto('/portfolio/greenline-lawn-and-grounds');await page.getByLabel('Lot size in square feet').fill('5000');await page.getByRole('button',{name:'Calculate sample'}).click();await expect(page.getByText(/Sample starts at \$/)).toBeVisible();expect(posts).toEqual([])});
