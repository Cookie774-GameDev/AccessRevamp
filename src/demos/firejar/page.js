import { demoShell } from '../shared/demo-shell.js'; import { PRODUCTS } from './data.js';
export function page() { const cards = PRODUCTS.map((p) => `<article class="product-card" data-product data-heat="${p.heat}"><div class="jar-art jar-art--${p.id}" aria-hidden="true"><span>${'●'.repeat(p.heat)}${'○'.repeat(3-p.heat)}</span></div><h2>${p.name}</h2><p>Heat ${p.heat}/3 · sample $${p.price}</p><p>${p.note}</p><button data-add="${p.id}" type="button">Add to demo cart</button></article>`).join(''); return demoShell({ name: 'Firejar Spicy Peanut Butter', purpose: 'A product discovery, cart, and checkout-state demonstration', rationale: 'Heat level is treated as a useful browsing attribute while product caveats stay beside the decision.', accessibilityNotes: 'Filters are real buttons with pressed state; cart changes are announced; checkout remains a safe local simulation.', body: `<section class="demo-hero demo-hero--fire"><p class="eyebrow">Fictional pantry experiment</p><h1>Sweet first. Heat with a point.</h1><p>Explore three invented flavors using original CSS artwork—no borrowed packaging or food photography.</p></section><section class="filter-row" aria-label="Filter by heat"><button data-heat-filter="all" aria-pressed="true">All heat</button><button data-heat-filter="1" aria-pressed="false">Gentle</button><button data-heat-filter="2" aria-pressed="false">Bright</button><button data-heat-filter="3" aria-pressed="false">Hot</button></section><section class="product-grid">${cards}</section><aside class="demo-cart" aria-labelledby="cart-title"><h2 id="cart-title">Demo cart <span data-cart-count>0</span></h2><div data-cart-items><p>Your sample cart is empty.</p></div><p data-cart-total>Sample total: $0</p><button data-demo-checkout type="button">Try demo checkout</button><p data-cart-status role="status" aria-live="polite"></p></aside>` }); }
