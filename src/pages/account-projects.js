import { shell } from '../components/shell.js';

export function accountProjectsPage({ pathname = '/account/projects' } = {}) {
  return shell(`<section class="customer-hub-page"><div class="container-wide"><header class="customer-workspace-head"><div><span class="eyebrow">Project workspace</span><h1>Your AccessRevamp projects</h1><p data-account-greeting>Checking your confirmed session…</p></div><div class="customer-workspace-head__account" data-account-profile><span class="micro-label">Profile</span><strong data-account-profile-name>Your account</strong><button class="button button--ghost button--small" type="button" data-account-logout hidden>Sign out</button></div></header><div data-account-state="loading" data-account-content role="region" aria-live="polite"><div class="loading-card"><p>Loading project progress, questions, designs, and private files…</p></div></div><aside class="notice customer-hub-security"><strong>Private by default.</strong> Project files use expiring download links. Never share your account password or private project links.</aside></div></section>`, { pathname });
}
