import { icon } from '../components/icons.js';
import { shell } from '../components/shell.js';

const finding = (number, title, impact, evidence, direction, severity) => `<article class="report-finding"><span>${number}</span><div><h3>${title}</h3><p><strong>Impact:</strong> ${impact}</p><p><strong>Evidence:</strong> ${evidence}</p><p><strong>Direction:</strong> ${direction}</p></div><span class="severity">${severity}</span></article>`;

export function sampleReportPage() {
  return shell(`<section class="page-hero"><div class="container-wide page-hero__split"><div><span class="eyebrow">Illustrative sample report</span><h1>Evidence first. Recommendations second.</h1></div><div class="page-hero__aside"><p class="lede">Northline Goods is a fictional portfolio concept. This sample demonstrates format and reasoning; it is not a completed audit of a real business.</p></div></div></section><section class="section"><div class="container-wide report-preview"><aside class="report-preview__intro"><div><span class="micro-label">Sample · public homepage</span><h2>One promise, one proof point, one action.</h2><p>Observations are separated from anything that still needs verification. Priorities reflect likely visitor impact, confidence, and dependency.</p></div><a class="button button--ink" href="/pricing" data-nav>Choose a plan ${icon('arrow')}</a></aside><div class="report-preview__sheet">${finding('01', 'The primary action competes with navigation', 'Visitors may not know whether to shop, learn, or subscribe first.', 'Visual hierarchy review at desktop and mobile widths.', 'Use one dominant action and demote supporting links.', 'High')}${finding('02', 'Muted copy needs contrast verification', 'Important context may be difficult for some visitors to read.', 'Final computed colors and states still require measurement.', 'Raise foreground contrast and verify hover, focus, and disabled states.', 'Verify')}${finding('03', 'Trust arrives after the first decision', 'Shipping and material proof cannot reduce uncertainty when they appear too late.', 'Content-order review of the public purchase path.', 'Bring one concise reassurance into the first-screen composition.', 'Plan')}<div class="notice"><strong>Boundary:</strong> AccessRevamp is not a penetration test, legal certification, or guarantee of compliance, security, or revenue.</div></div></div></section>`, { pathname: '/sample-report' });
}
